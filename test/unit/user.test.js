describe('logistics_server_user test', function() {
	/**
	 * pre condition
	 * similar to setup
	 */
	beforeEach(function() {
	});

	/**
	 * after this module'test
	 * similar to teardown
	 */
	afterEach(function() {

	});
	it('', function() {
	});
});
